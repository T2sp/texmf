\newcommand{\lev}[2]{
    % ノード b の座標を取得
    \path (#1); \pgfgetlastxy{\bx}{\by};

    % ノード c の座標を取得
    \path (#2); \pgfgetlastxy{\cx}{\cy};

    % b と c の座標差 (pt) を計算
    \pgfmathsetmacro{\dx}{\cx-\bx}
    \pgfmathsetmacro{\dy}{\cy-\by}

    \pgfmathsetmacro{\ax}{\bx + 0.5*(\dx) - sqrt(3)/2 * \dy}
    \pgfmathsetmacro{\ay}{\by + 0.5*(\dy) + sqrt(3)/2 * \dx}


    \node[above] (a) at (\ax pt, \ay pt) {};
    \node[] (x) at (\bx, \ay pt) {};
    \node[] (y) at (\cx, \ay pt) {};

    \draw[->-=0.2,->-=0.85] (#2) .. controls (y) and (x) .. (#1);
}

\newcommand{\rev}[2]{
    % ノード b の座標を取得
    \path (#1); \pgfgetlastxy{\bx}{\by};

    % ノード c の座標を取得
    \path (#2); \pgfgetlastxy{\cx}{\cy};

    % b と c の座標差 (pt) を計算
    \pgfmathsetmacro{\dx}{\cx-\bx}
    \pgfmathsetmacro{\dy}{\cy-\by}

    \pgfmathsetmacro{\ax}{\bx + 0.5*(\dx) - sqrt(3)/2 * \dy}
    \pgfmathsetmacro{\ay}{\by + 0.5*(\dy) + sqrt(3)/2 * \dx}


    \node[above] (a) at (\ax pt, \ay pt) {};
    \node[] (x) at (\bx, \ay pt) {};
    \node[] (y) at (\cx, \ay pt) {};

    \draw[->-=0.2,->-=0.85] (#1) .. controls (x) and (y) .. (#2);
}

\newcommand{\lcoev}[2]{
    % ノード b の座標を取得
    \path (#1); \pgfgetlastxy{\bx}{\by};

    % ノード c の座標を取得
    \path (#2); \pgfgetlastxy{\cx}{\cy};

    % b と c の座標差 (pt) を計算
    \pgfmathsetmacro{\dx}{\cx-\bx}
    \pgfmathsetmacro{\dy}{\cy-\by}

    \pgfmathsetmacro{\ax}{\bx + 0.5*(\dx) + sqrt(3)/2 * \dy}
    \pgfmathsetmacro{\ay}{\by + 0.5*(\dy) - sqrt(3)/2 * \dx}


    \node[below] (a) at (\ax pt, \ay pt) {};
    \node[] (x) at (\bx, \ay pt) {};
    \node[] (y) at (\cx, \ay pt) {};

    \draw[->-=0.2,->-=0.85] (#2) .. controls (y) and (x) .. (#1);
}

\newcommand{\rcoev}[2]{
    % ノード b の座標を取得
    \path (#1); \pgfgetlastxy{\bx}{\by};

    % ノード c の座標を取得
    \path (#2); \pgfgetlastxy{\cx}{\cy};

    % b と c の座標差 (pt) を計算
    \pgfmathsetmacro{\dx}{\cx-\bx}
    \pgfmathsetmacro{\dy}{\cy-\by}

    \pgfmathsetmacro{\ax}{\bx + 0.5*(\dx) + sqrt(3)/2 * \dy}
    \pgfmathsetmacro{\ay}{\by + 0.5*(\dy) - sqrt(3)/2 * \dx}


    \node[below] (a) at (\ax pt, \ay pt) {};
    \node[] (x) at (\bx, \ay pt) {};
    \node[] (y) at (\cx, \ay pt) {};

    \draw[->-=0.2,->-=0.85] (#1) .. controls (x) and (y) .. (#2);
}